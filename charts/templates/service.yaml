apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-ui.fullname" . }}
  labels:
    {{- include "x-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "x-ui.selectorLabels" . | nindent 4 }}
